---
description: Run this repo's full verification — typecheck, lint where present, tests, and lock-file check
---

Run every check CI runs for this repository, plus the lock-file check CI
cannot do for you. Do not stop at the first failure — collect all results so
one report covers everything.

```bash
npx tsc --noEmit
npx eslint .
npm test
```

`eslint .` currently reports pre-existing errors — report the count and whether it rose, not a full dump. CI runs typecheck, tests and build but not lint.

## Lock file matches CI

CI runs Node 20 / npm 10. A lock generated by a newer npm fails `npm ci` with
`Missing: <pkg> from lock file`, even though the local install worked.

```bash
npx -y npm@10 ci --dry-run
```

If it fails, regenerate with CI's npm and re-verify:

```bash
npx -y npm@10 install --package-lock-only
```

## No emoji

```bash
rg -n "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{FE0F}\x{2B00}-\x{2BFF}]" --glob '!**/{node_modules,dist,build,android,ios}/**'
```

Zero matches expected.

## Report

A short table: check, pass or fail. Then the details of anything that failed,
with file and line. State plainly whether this is safe to push. If something
failed, do not describe it as passing.

